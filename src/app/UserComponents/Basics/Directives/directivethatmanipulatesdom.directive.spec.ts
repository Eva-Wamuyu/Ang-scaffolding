import { DirectivethatmanipulatesdomDirective } from './directivethatmanipulatesdom.directive';

describe('DirectivethatmanipulatesdomDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectivethatmanipulatesdomDirective();
    expect(directive).toBeTruthy();
  });
});
